// regular function
function addOne(num :number) {
    return num + 1;
}

const res = addOne(3);
console.log(res);

// arrow function 
const double = (x: number, y: number) =>  x * y;

const result = double(5, 8);
console.log(result)
