// tuple implements on array that define each index fixed type

let myTuple : [number , string] ;
myTuple = [10, 'Hello world'];

//error
// myTuple = ['Hello world', 10];

console.log(myTuple);

const games: [string, number, boolean] = ['Games', 1, true];

console.log(games)

